export const ContentLayout = ({ children }: { children: React.ReactNode }) => {
  return <div className="mx-auto mt-4 max-h-full w-full overflow-y-auto px-2 py-2 sm:max-w-screen-sm">{children}</div>
}
